# ADR-0008 — OpenAPI as Python/TypeScript Transport Authority
Status: ACCEPTED_CANDIDATE

Decision: FastAPI transport generates OpenAPI; frontend TypeScript client/types are generated from the contract. Manually duplicated canonical API DTO definitions across Python and TypeScript are prohibited.