# ADR-0003 — Python Intelligence / TypeScript Presentation
Status: ACCEPTED_CANDIDATE

Decision: Python 3.14 standard CPython owns ingestion interpretation, domain logic, provenance, clustering/trend algorithms, replay and API application logic. TypeScript strict + React owns presentation. Full TypeScript V0 rejected; Go acquisition deferred. Reopen only through measured reversal gates.