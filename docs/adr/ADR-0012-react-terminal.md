# ADR-0012 — React Terminal / Static Web Plane
Status: ACCEPTED_CANDIDATE

Decision: TypeScript strict + React 19 + Vite; React Router owns navigation; TanStack Query owns server state; TanStack Table/Virtual support dense surfaces. Chart engine is not permanently frozen. Reversal requires measured operator-task/accessibility/performance failure after reasonable optimization.